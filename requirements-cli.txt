# Install alongside requirements.txt for local CLI use (adds chart/report generation):
#   pip install -r requirements.txt -r requirements-cli.txt --break-system-packages
matplotlib>=3.7.0
jinja2>=3.1.0
google-auth-oauthlib>=1.2.0
